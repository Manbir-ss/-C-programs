#include<stdio.h>

int main()

{
    int sl,yr,ug=0,pg=1;
    char gr,g;

    printf("Enter the age, Graduation (ug=0 pg=1), gender, year of service");
    scanf("%d%c%c%d",&yr,&gr,&g,&sl);

    if ((yr>=10)&&(gr=0)&&(g='M'))
        sl=15000
    else if ((yr>=10)&&(gr=1)&&(g='M'))
        sl=10000
    else if ((yr))
}
